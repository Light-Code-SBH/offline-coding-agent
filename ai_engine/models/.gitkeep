# This file keeps the models directory in git even though model files are gitignored.
# AI model files (.gguf, .bin, etc.) are too large for Git.
# Download models using: python ai_engine/download_model.py
